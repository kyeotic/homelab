---
# Install common packages on Proxmox host

- name: install packages
  apt:
    name:
      - htop
      - sysstat
      - radeontop
      - acl
      - jq
    state: present
    update_cache: yes
    cache_valid_time: 3600
