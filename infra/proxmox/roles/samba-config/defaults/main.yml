---
# Samba configuration

samba_user: samba
samba_bws_secret_id: ""  # Bitwarden secret ID for samba password

# Samba share definitions
samba_shares:
  - name: nas
    path: /mnt/nas
    comment: "NAS Share"
    read_only: false
    browseable: true
